# 화면 구성

## 개요

claude 명령어 감지는 서버 측 폴링 로직이므로 직접적인 UI 요소가 없다. 감지 결과에 따른 Panel 타입 자동 전환의 시각적 효과를 정의한다.

## 자동 전환 시 시각 효과

### Terminal → Claude Code 자동 전환

```
BEFORE (Terminal 모드):
┌──────────────────────────────────────────┐
│ [Tab1]                    [─] [│] [✕]    │
├──────────────────────────────────────────┤
│ $ claude                                 │
│ ╭────────────────────────────────────╮   │
│ │ > 버그 수정해줘                     │   │
│ ╰────────────────────────────────────╯   │
│                                          │
└──────────────────────────────────────────┘

    ↓ claude 명령어 감지 → panelType 자동 전환

AFTER (Claude Code 모드):
┌──────────────────────────────────────────┐
│ [Tab1]                 [⬚] [─] [│] [✕]  │
├──────────────────────────────────────────┤
│  ┌─ 타임라인 ─────────────────────────┐  │
│  │ (세션 데이터 로드 중 → 스켈레톤)    │  │
│  │ ...                                │  │
│  └────────────────────────────────────┘  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  ┌─ 터미널 (축소) ────────────────────┐  │
│  │ $ claude                           │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

- 전환 애니메이션: 타임라인 영역이 상단에서 슬라이드 인 (150ms ease-out)
- 터미널 영역: 전체 → 축소로 부드럽게 전환
- 탭 바: Panel 전환 토글 아이콘 자동 추가

### claude 종료 후

- Panel 타입 `claude-code` 유지
- 타임라인은 마지막 상태 그대로 표시 (정적)
- 사용자가 수동으로 Terminal 모드로 전환 가능

## 감지 상태 표시

감지 동작 자체는 백그라운드에서 이루어지며, 사용자에게 폴링 상태를 표시하지 않는다. 전환 결과만 시각적으로 반영된다.

## 파일 구조

```
src/
└── lib/
    └── claude-detection.ts   ← tmux 프로세스 폴링 로직
```
