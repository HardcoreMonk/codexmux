# Release v0.4.6 Conditional Batch Handoff

Date: 2026-05-06

## Release And Deploy

- `corepack pnpm release:patch` completed `v0.4.6`.
- Feature commit included in the release push: `948a920 feat: add backlog completion gate`.
- Release commit: `0968ac1 chore: release v0.4.6`.
- Tag pushed: `v0.4.6`.
- Release script verification passed: `lint`, `test` 141 files / 684 tests, `tsc --noEmit`, `build`.
- `corepack pnpm deploy:local` completed local build and `systemctl --user restart codexmux.service`.
- Live health after deploy: `version=0.4.6`, `commit=0968ac1`, `buildTime=2026-05-06T13:36:42.191Z`.
- `codexmux.service`: `ActiveState=active`, `SubState=running`, `Result=success`, `NRestarts=0`, `ExecMainPID=2597158`.
- Runtime v2 Phase 6 gate passed with terminal `new-tabs`, storage/timeline/status `default`, and clean worker counters.

## Android Device Evidence

Device: SM-S928N, Android 16, package `com.hardcoremonk.codexmux`.

| Check | Result |
| --- | --- |
| `corepack pnpm android:build:debug` | passed |
| `corepack pnpm android:install` | passed |
| `corepack pnpm smoke:android:install` | passed, `versionName=0.4.6`, `versionCode=406` |
| `corepack pnpm smoke:android:foreground` | passed, 2 foreground rounds, blocking console/logcat 0 |
| `corepack pnpm smoke:android:runtime-v2` | passed, initial + 2 foreground runtime v2 terminal markers, blocking console/logcat 0 |
| `corepack pnpm smoke:android:recovery` | passed, network/http/ssl recovery, final Tailscale URL, blocking console/logcat 0 |
| `corepack pnpm android:bundle:release` | passed |
| `corepack pnpm smoke:android:release-aab` | passed, fresh signed AAB, `versionName=0.4.6`, `versionCode=406`, bytes `3030140` |

Android timeline foreground status:

- First default `corepack pnpm smoke:android:timeline-foreground` reached timeline checks through
  `timeline-initial-init`, `timeline-foreground-1`, `timeline-foreground-2`, and
  `console-logcat-clean`, then failed the final restore-to-Tailscale step.
- Retry failed earlier with `CDP Runtime.evaluate timed out` on the temp dev server path.
- Recovery smoke passed afterward, so launcher URL recovery is still independently covered.
- Keep this row open as `manual-required` or retry in a clean Android/WebView window before using it
  as completion-gate evidence.

## iPad And Mac UX Evidence

| Check | Result |
| --- | --- |
| `corepack pnpm smoke:pwa` | passed; live health `0.4.6/0968ac1`, manifest, icons, splash assets, service worker, and iPad viewport browser probe clean |
| `corepack pnpm build:electron` | passed on Linux x86_64 |

Real iPad Home Screen long-background, input draft preservation, and macOS packaged
Finder/Gatekeeper UX still require their actual devices/sessions. This Linux host cannot produce
those manual UX artifacts.

## Spec-Required Items

Spec-required backlog items remain started under
`docs/superpowers/specs/2026-05-06-spec-required-backlog-kickoff.md`. This batch did not implement
rollback mutation, durable runtime source-of-truth migration, durable approval audit UI, timeline
windowing, status adaptive scheduling, fork/sub-agent UI, resume failure taxonomy, Codex state
SQLite indexing, or app-server provider adapter code.
