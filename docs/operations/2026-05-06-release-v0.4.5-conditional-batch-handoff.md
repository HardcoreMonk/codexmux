# Release v0.4.5 Conditional Batch Handoff

Date: 2026-05-06

## Release And Deploy

- `corepack pnpm release:patch` completed `v0.4.5`.
- Release commit: `1f63048 chore: release v0.4.5`.
- Tag pushed: `v0.4.5`.
- Release script verification passed: `lint`, `test` 140 files / 680 tests, `tsc --noEmit`, `build`.
- `corepack pnpm deploy:local` completed local build and `systemctl --user restart codexmux.service`.
- Live health after deploy: `version=0.4.5`, `commit=1f63048`, `buildTime=2026-05-06T12:55:14.320Z`.
- `codexmux.service`: `ActiveState=active`, `SubState=running`, `Result=success`, `NRestarts=0`, `ExecMainPID=2241800`.
- Runtime v2 Phase 6 gate passed with terminal `new-tabs`, storage/timeline/status `default`, and clean worker counters.

## Android Device Evidence

Device: SM-S928N, Android 16, package `com.hardcoremonk.codexmux`.

| Check | Result |
| --- | --- |
| `corepack pnpm android:build:debug` | passed |
| `corepack pnpm android:install` | passed |
| `corepack pnpm smoke:android:install` | passed, `versionName=0.4.5`, `versionCode=405` |
| `corepack pnpm smoke:android:foreground` | passed, 2 foreground rounds, blocking console/logcat 0 |
| `corepack pnpm smoke:android:runtime-v2` | passed, initial + 2 foreground runtime v2 terminal markers, blocking console/logcat 0 |
| `corepack pnpm smoke:android:timeline-foreground` | passed, timeline default, entries 3/5/7, blocking console/logcat 0 |
| `corepack pnpm smoke:android:recovery` | passed, network/http/ssl recovery, blocking console/logcat 0 |
| `corepack pnpm android:bundle:release` | passed |
| `corepack pnpm smoke:android:release-aab` | passed, fresh signed AAB, `versionName=0.4.5`, `versionCode=405` |

## iPad And Mac UX Evidence

| Check | Result |
| --- | --- |
| `corepack pnpm smoke:pwa` | passed; manifest, icons, splash assets, service worker, iPad viewport browser probe clean |
| `corepack pnpm build:electron` | passed on Linux x86_64 |

Real iPad Home Screen long-background, input draft preservation, and macOS packaged
Finder/Gatekeeper UX still require their actual devices/sessions. This Linux host cannot produce
those manual UX artifacts.

## Spec-Required Items

Spec-required items remain started under
`docs/superpowers/specs/2026-05-06-spec-required-backlog-kickoff.md`. This batch did not implement
rollback mutation, durable runtime source-of-truth migration, durable approval audit UI, timeline
windowing, status adaptive scheduling, fork/sub-agent UI, resume failure taxonomy, Codex state
SQLite indexing, or app-server provider adapter code.
