# Release v0.4.4 Operational Batch Handoff

Date: 2026-05-06

## Release And Deploy

- `corepack pnpm release:patch` completed `v0.4.4`.
- Release commit: `3dfbc04 chore: release v0.4.4`.
- Tag pushed: `v0.4.4`.
- Release script verification passed: `lint`, `test` 139 files / 676 tests, `tsc --noEmit`, `build`.
- `corepack pnpm deploy:local` completed local build and `systemctl --user restart codexmux.service`.
- Live health after deploy: `version=0.4.4`, `commit=3dfbc04`, `buildTime=2026-05-06T12:22:17.212Z`.
- `codexmux.service`: `ActiveState=active`, `SubState=running`, `Result=success`, `NRestarts=0`, `ExecMainPID=2107376`.
- Runtime v2 Phase 6 gate passed with terminal `new-tabs`, storage/timeline/status `default`, and clean worker counters.

## Android Device Evidence

Device: SM-S928N, Android 16, package `com.hardcoremonk.codexmux`.

| Check | Result |
| --- | --- |
| `corepack pnpm android:build:debug` | passed |
| `corepack pnpm android:install` | passed |
| `corepack pnpm smoke:android:install` | passed, `versionName=0.4.4`, `versionCode=404` |
| `corepack pnpm smoke:android:foreground` | passed, 2 foreground rounds, blocking console/logcat 0 |
| `corepack pnpm smoke:android:runtime-v2` | passed, initial + 2 foreground runtime v2 terminal markers, blocking console/logcat 0 |
| `corepack pnpm smoke:android:timeline-foreground` | passed, timeline default, entries 3/5/7, blocking console/logcat 0 |
| `corepack pnpm smoke:android:recovery` | passed, network/http/ssl recovery, blocking console/logcat 0 |
| `corepack pnpm android:bundle:release` | passed |
| `corepack pnpm smoke:android:release-aab` | passed, fresh signed AAB, `versionName=0.4.4`, `versionCode=404` |

## iPad And Mac UX Evidence

| Check | Result |
| --- | --- |
| `corepack pnpm smoke:pwa` | passed after sequential rerun; manifest, icons, splash assets, service worker, iPad viewport browser probe clean |
| `corepack pnpm build:electron` | passed on Linux x86_64 |

The first `smoke:pwa` attempt ran concurrently with `build:electron` and saw a transient
`/android-chrome-512x512.png` 400 while build output was being rewritten. Direct fetch then returned
200, and sequential `smoke:pwa` passed.

Real iPad Home Screen long-background, input draft preservation, and macOS packaged
Finder/Gatekeeper UX still require their actual devices/sessions. This Linux host cannot produce
those manual UX artifacts.

## Spec-Required Kickoff

Created `docs/superpowers/specs/2026-05-06-spec-required-backlog-kickoff.md` covering:

- Runtime rollback mutation and systemd drop-in editing.
- Durable runtime state source of truth.
- Approval durable audit history.
- Timeline windowed render.
- Status adaptive scheduling.
- Fork/sub-agent relationship UI.
- Codex resume failure taxonomy.
- Codex state SQLite read-only indexer.
- App-server provider adapter.

No implementation code was added for these spec-required items in this batch.
